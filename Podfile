# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NESKit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NESKit
  pod 'SwiftLint'
  target 'NESKitTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'swift-nes' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swift-nes

end
